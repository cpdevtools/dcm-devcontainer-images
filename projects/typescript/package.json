{
  "name": "typescript",
  "publishConfig": {
    "type": "docker"
  },
  "scripts": {
    "actions-build": "devcontainer build  --workspace-folder ./ --image-name typescript:latest",
    "actions-publish": "devcontainer build  --workspace-folder ./ --image-name typescript:latest"
  }
}
