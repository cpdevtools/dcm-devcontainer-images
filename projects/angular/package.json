{
  "name": "angular",
  "publishConfig": {
    "type": "docker"
  },
  "scripts": {
    "actions-build": "devcontainer build  --workspace-folder ./ --image-name angular:latest",
    "actions-publish": "devcontainer build  --workspace-folder ./ --image-name angular:latest"
  }
}
