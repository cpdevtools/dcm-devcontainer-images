{
  "name": "angular-dc-image",
  "publishConfig": {
    "type": "docker"
  },
  "scripts": {
    "actions-build": "devcontainer build  --workspace-folder ./ --image-name ghcr.io/cpdevtools/angular-dc-image:${VERSION:-latest}",
    "actions-publish": "devcontainer build  --workspace-folder ./ --image-name ghcr.io/cpdevtools/angular-dc-image:${VERSION:-latest}"
  }
}
