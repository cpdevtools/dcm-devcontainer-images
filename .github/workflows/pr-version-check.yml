name: Check that the PR version is valid

on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - "release/*"

jobs:
  build-dotnet:
    name: dotnet
    uses: cpdevtools/common-github-workflows/.github/workflows/pnpm-build-and-test-projects.yml@main
    with:
      projectFilter: "dotnet"
